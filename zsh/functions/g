# No arguments: `git status`
# With arguments: acts like `git`
g() {
  if [ -d ".hg" ]; then
    hg $@
  elif [[ $# > 0 ]]; then
    git $@
  else
    git l -4
    git s
  fi
}

# Complete g like git
compdef g=git
